{
  "name": "itella-mapping",
  "version": "1.1.0",
  "description": "Itella pickup points map and selection",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.8.3",
    "@babel/core": "^7.8.3",
    "@babel/preset-env": "^7.8.3",
    "autoprefixer": "^9.7.3",
    "live-server": "^1.2.1",
    "postcss-cli": "^6.1.3"
  },
  "scripts": {
    "start": "live-server --no-browser --watch=example,src --entry-file=example/index.html --mount=/images:./src/images",
    "autoprefixer": "postcss src/*.css -m --use autoprefixer -d dist/",
    "build": "babel src -d dist && npm run autoprefixer",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "itella"
  ],
  "author": "",
  "license": "ISC"
}
