{
  "name": "itella-mapping",
  "version": "1.3.2",
  "description": "Itella pickup points map and selection",
  "repository": {
    "url": "https://github.com/ItellaPlugins/itella-mapping-js"
  },
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/preset-env": "^7.11.5",
    "autoprefixer": "^9.8.6",
    "live-server": "^1.2.1",
    "postcss-cli": "^7.1.2"
  },
  "scripts": {
    "start": "live-server --no-browser --watch=example,src --entry-file=example/index.html --mount=/images:./src/images",
    "autoprefixer": "postcss src/*.css -m --use autoprefixer -d dist/",
    "build": "babel src -d dist && npm run autoprefixer",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "itella"
  ],
  "author": "",
  "license": "ISC"
}
